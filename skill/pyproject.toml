[project]
name = "pounding-ozon-probe"
version = "0.1.0"
description = "1688 API + CDP browser probe → GraphInput envelope assembler"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.28",
    "playwright>=1.45",
]

[project.scripts]
pounding-probe = "scripts.cli:main"
